

Процедура ОткрытьИнструментыПриСтартеСистемы()
	
	Декоратор = Обработки.xddDecorator.Создать();
	Декоратор.Инициализация();
	
	ПараметрыЗапуска = Декоратор.РазобратьПараметрыЗапуска(ПараметрЗапуска);
	Если НЕ ЗначениеЗаполнено(ПараметрыЗапуска) ИЛИ ПустаяСтрока(ПараметрыЗапуска.ПутьОтчета) Тогда
		Возврат;
	КонецЕсли;
	
	Отчет = Декоратор.ПолучитьОтчетОПрогонеТестов();
	
	ИмяФайла = ПараметрыЗапуска.ПутьОтчета + "\" + ПараметрыЗапуска.ФорматОтчета;
	Отчет.Записать(ИмяФайла, ТипФайлаТабличногоДокумента.XLS);
	
КонецПроцедуры

ОткрытьИнструментыПриСтартеСистемы();



