
Перем мФормаОтладочнойИнформации;

Процедура ДанныеДляОтображения_Добавить(Данные, Источник)
	
	НоваяСтрока = ДанныеДляОтображения.Добавить();
	НоваяСтрока.Источник = Источник;
	НоваяСтрока.Данные = Данные;
	
КонецПроцедуры

Процедура ВывестиОтладочнуюИнформацию(Данные, ОписаниеДанных) Экспорт
	
	ДанныеДляОтображения_Добавить(Данные, ОписаниеДанных);
	
	Если мФормаОтладочнойИнформации = Неопределено Тогда
		мФормаОтладочнойИнформации = Обработки.xddMonitor.ПолучитьФорму();
	КонецЕсли;
	
	ЗначениеВДанныеФормы(ЭтотОбъект, мФормаОтладочнойИнформации.Объект);
	мФормаОтладочнойИнформации.Открыть();
	
	
КонецПроцедуры

ДанныеДляОтображения.Колонки.Добавить("Источник", Новый ОписаниеТипов("Строка"));
ДанныеДляОтображения.Колонки.Добавить("Данные"); // в этом поле может быть табличный документ, таблица значений или что-нибудь еще


